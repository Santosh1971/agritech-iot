# Copy to secrets.properties (gitignored) and fill in the real values.
# MQTT_USER / MQTT_PASS: the shared FG1 broker account the app uses to check devices.
# SOFTAP_PASSWORD: must match SOFTAP_PASSWORD in the FG1 firmware's include/Config.h.
MQTT_USER=
MQTT_PASS=
SOFTAP_PASSWORD=
